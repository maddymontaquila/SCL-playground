Playground for trying out S.CL and Spectre.Console features

To publish the tool as AOT, run the pwsh or sh scripts, then cd into the "publish" folder.

Root command - `aspire`
Sub commands - `dev`, `new`, `add`
